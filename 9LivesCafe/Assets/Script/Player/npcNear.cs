using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class npcNear : MonoBehaviour
{
    public float radarDistance = 5f;

    //Returns closest NPC if an NPC is near player, else NULL.
    NPC closestNPC()
    {
        GameObject[] nearObj;
        NPC closest = null;

        


        return closest;
    }
}
